export class Appointment {
    constructor(public id ?: number, public patientId?: number, public doctorId?: number, public description?: string, public date?: string, public timing?: string, public status?: boolean ) {
        
    }
    
      
}
